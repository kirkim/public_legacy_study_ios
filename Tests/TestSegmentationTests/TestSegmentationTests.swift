import XCTest
@testable import TestSegmentation

final class TestSegmentationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TestSegmentation().text, "Hello, World!")
    }
}
